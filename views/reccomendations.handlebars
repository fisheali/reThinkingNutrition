<a href="/">
<span id="menu_span">
<p id='menu_logo'><img src='../images/logo.webp'></p>
</span>
</a>

<form class="" action="/chosenRecommendation">
  {{#each reccomendations}}
  {{#each type}}
  <input type="checkbox" name="{{this}}" value="{{this}}"> <lable for={{this}}> {{this}} </lable>
  {{/each}}
  {{/each}}
  <input type="submit" value="Submit">
</form>
